# auto-generated by grc.converter

id: rds_panel
label: RDS Panel
category: '[RDS]'

parameters:
-   id: freq
    label: Frequency
    dtype: real
    default: '0'
    hide: part
-   id: gui_hint
    label: GUI Hint
    dtype: gui_hint
    hide: part

inputs:
-   domain: message
    id: in

templates:
    imports: |-
        import rds
    callbacks:
    - set_frequency(${freq})
    make: |-
        <%
            win = 'self._%s_win'%id
        %>\
        rds.rdsPanel(${freq})
        ${win} = self.${id}
        ${gui_hint() % win}

documentation: |-
    Any change in Frequency parameter will trigger clearing of panel data and set corresponding text on panel.

file_format: 1
