# auto-generated by grc.converter

id: gsm_decryption
label: Decryption

parameters:
-   id: k_c
    label: Kc session key
    dtype: int_vector
    default: '[0,0,0,0,0,0,0,0]'
-   id: a5_version
    label: A5 version
    dtype: int
    default: '1'

inputs:
-   domain: message
    id: bursts

outputs:
-   domain: message
    id: bursts
asserts:
- ${ a5_version > 0 }
- ${ a5_version < 5 }

templates:
    imports: from gnuradio import gsm
    make: gsm.decryption(${k_c}, ${a5_version})

file_format: 1
