# auto-generated by grc.converter

id: gsm_uplink_downlink_splitter
label: Up/Down-link splitter

inputs:
-   domain: message
    id: in
    optional: true

outputs:
-   domain: message
    id: downlink
    optional: true
-   domain: message
    id: uplink
    optional: true

templates:
    imports: from gnuradio import gsm
    make: gsm.uplink_downlink_splitter()

file_format: 1
