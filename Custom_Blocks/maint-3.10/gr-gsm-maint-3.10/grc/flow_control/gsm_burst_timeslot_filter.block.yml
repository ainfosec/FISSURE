# auto-generated by grc.converter

id: gsm_burst_timeslot_filter
label: Burst Timeslot Filter

parameters:
-   id: timeslot
    label: Timeslot
    dtype: int
    default: '0'
    hide: none

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out
    optional: true

templates:
    imports: from gnuradio import gsm
    make: gsm.burst_timeslot_filter(${timeslot})

documentation: |-
    This block forwards only bursts in the timeslot given by the parameter

file_format: 1
