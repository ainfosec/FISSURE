# auto-generated by grc.converter

id: gsm_controlled_rotator_cc
label: Controlled Rotator

parameters:
-   id: phase_inc
    label: phase_inc
    dtype: real
    default: '0'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: from gnuradio import gsm
    make: gsm.controlled_rotator_cc(${phase_inc})
    callbacks:
    - set_phase_inc(${phase_inc})

file_format: 1
