# auto-generated by grc.converter

id: ieee802_11_mac
label: WiFi MAC
category: '[IEEE802.11]'

parameters:
-   id: src_mac
    label: SRC MAC
    dtype: int_vector
    default: '[0x23, 0x23, 0x23, 0x23, 0x23, 0x23]'
-   id: dst_mac
    label: DST MAC
    dtype: int_vector
    default: '[0x42, 0x42, 0x42, 0x42, 0x42, 0x42]'
-   id: bss_mac
    label: BSS MAC
    dtype: int_vector
    default: '[0xff, 0xff, 0xff, 0xff, 0xff, 0xff]'

inputs:
-   domain: message
    id: app in
    optional: true
-   domain: message
    id: phy in
    optional: true

outputs:
-   domain: message
    id: app out
    optional: true
-   domain: message
    id: phy out
    optional: true
asserts:
- ${ len(src_mac) == 6 }
- ${ len(dst_mac) == 6 }
- ${ len(bss_mac) == 6 }
- ${ all([x >= 0 and 255 >= x for x in src_mac]) }
- ${ all([x >= 0 and 255 >= x for x in dst_mac]) }
- ${ all([x >= 0 and 255 >= x for x in bss_mac]) }

templates:
    imports: import ieee802_11
    make: ieee802_11.mac(${src_mac}, ${dst_mac}, ${bss_mac})

file_format: 1
