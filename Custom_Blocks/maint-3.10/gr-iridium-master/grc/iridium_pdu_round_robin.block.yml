# auto-generated by grc.converter

id: iridium_pdu_round_robin
label: pdu_round_robin
category: '[Iridium]'

parameters:
-   id: output_count
    label: Output Count
    dtype: int
    default: '2'

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out_0
-   domain: message
    id: out_1

templates:
    imports: import iridium
    make: iridium.pdu_round_robin(${output_count})

file_format: 1
