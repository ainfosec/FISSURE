# auto-generated by grc.converter

id: iridium_iridium_qpsk_demod
label: iridium_qpsk_demod
category: '[Iridium]'

parameters:
-   id: channels
    label: Channels
    dtype: int
    default: '1'

inputs:
-   domain: message
    id: cpdus
    multiplicity: ${channel}

outputs:
-   domain: message
    id: pdus

templates:
    imports: import iridium
    make: "iridium.iridium_qpsk_demod(${channels})"

file_format: 1
