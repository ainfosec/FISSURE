# auto-generated by grc.converter

id: console
label: Console
category: '[DECT2]'

parameters:
-   id: gui_hint
    label: GUI Hint
    dtype: gui_hint
    hide: part

inputs:
-   domain: message
    id: in
    optional: true

templates:
    imports: import gnuradio.dect2 as dect2
    #make: "<% win = 'self.%s'%id %>\ndect2.console()\n${gui_hint(in)}\n  "
    make: |-
        <%
            win = 'self.%s' % id
        %>\
        dect2.console()
        ${gui_hint() % win}
file_format: 1
