# auto-generated by grc.converter

id: ieee802_15_4_rime_stack
label: RIME Stack
category: '[IEEE802.15.4]'

parameters:
-   id: bc_channels
    label: Broadcast Channels
    dtype: int_vector
    default: '[129]'
    hide: part
-   id: uc_channels
    label: Unicast Channels
    dtype: int_vector
    default: '[130]'
    hide: part
-   id: ruc_channels
    label: Reliable Unicast Channels
    dtype: int_vector
    default: '[131]'
    hide: part
-   id: rime_add
    label: RIME Address
    dtype: int_vector
    default: '[23,42]'

inputs:
-   domain: message
    id: bcin
    multiplicity: ${ len(bc_channels) }
    optional: true
-   domain: message
    id: ucin
    multiplicity: ${ len(uc_channels) }
    optional: true
-   domain: message
    id: rucin
    multiplicity: ${ len(ruc_channels) }
    optional: true
-   domain: message
    id: fromMAC
    multiplicity: '1'

outputs:
-   domain: message
    id: bcout
    multiplicity: ${ len(bc_channels) }
    optional: true
-   domain: message
    id: ucout
    multiplicity: ${ len(uc_channels) }
    optional: true
-   domain: message
    id: rucout
    multiplicity: ${ len(ruc_channels) }
    optional: true
-   domain: message
    id: toMAC
    multiplicity: '1'

templates:
    imports: import ieee802_15_4
    make: ieee802_15_4.rime_stack(${bc_channels}, ${uc_channels}, ${ruc_channels},
        ${rime_add})

file_format: 1
