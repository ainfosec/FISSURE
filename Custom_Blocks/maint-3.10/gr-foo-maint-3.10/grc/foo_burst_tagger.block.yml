# auto-generated by grc.converter

id: foo_burst_tagger
label: Burst Tagger
category: '[Foo]'

parameters:
-   id: tag_name
    label: Tag Name
    dtype: raw
    default: pmt.intern("burst_len")
-   id: mult
    label: Multiplier
    dtype: int
    default: '1'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex
asserts:
- ${ mult > 0 }

templates:
    imports: |-
        import pmt
        import foo
    make: foo.burst_tagger(${tag_name}, ${mult})

file_format: 1
