# auto-generated by grc.converter

id: foo_rtt_measure
label: RTT measure
category: '[Foo]'

parameters:
-   id: interval
    label: Send interval
    dtype: int
    default: '500'

inputs:
-   domain: message
    id: in

outputs:
-   label: dout
    domain: stream
    dtype: byte
-   domain: message
    id: out

templates:
    imports: import foo
    make: foo.rtt_measure(${interval})

file_format: 1
